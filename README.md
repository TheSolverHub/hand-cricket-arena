# Hand Cricket Arena V38.1 Render Fix

This package includes the V38 server and HTML plus the missing package.json required by Render.

Render settings:
- Build Command: npm install
- Start Command: npm start
- Root Directory: leave blank when these files are at repository root.

Required root files:
- package.json
- server.js
- Hand_Cricket_Arena.html
