Hand Cricket Arena Unlimited Multiplayer PRO — V11 Registration + Toss Fix

Key fixes:
- toss.mp3 is non-looping and plays only once per toss.
- Toss audio is stopped immediately when the coin lands.
- Multiplayer toss has a one-shot guard.
- Registration is the first screen.
- Custom unique Game ID is requested and returned by the server.
- Multiplayer uses the authenticated Game ID automatically; player name field is hidden.
- Admin setup instructions are in ADMIN_SETUP.txt.
- Continue with Google / Continue with Number UI is included, but real OAuth/OTP needs provider credentials/configuration.
